@model IEnumerable<Auto_Insurance_Management_System.Models.Payment>
@{
    ViewData["Title"] = "Payments Management";
}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">
            <i class="fas fa-money-check me-2 text-primary"></i>
            Payments Management
        </h2>
        </div>
    </div>

    <div class="card shadow-lg mb-4">
        <div class="card-body">
            <form method="get" class="row g-3">
                <div class="col-md-4">
                    <input type="text" name="search" class="form-control" placeholder="Search payments..." value="@ViewBag.Search">
                </div>
                <div class="col-md-3">
                    <select class="form-select" name="status">
                        <option value="">All Statuses</option>
                        <option value="Completed" selected='@(ViewBag.Status == "Completed")'>Completed</option>
                        <option value="Pending" selected='@(ViewBag.Status == "Pending")'>Pending</option>
                        <option value="Failed" selected='@(ViewBag.Status == "Failed")'>Failed</option>
                        <option value="Declined" selected='@(ViewBag.Status == "Declined")'>Declined</option>
                        <option value="Refunded" selected='@(ViewBag.Status == "Refunded")'>Refunded</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="fas fa-filter me-2"></i> Filter
                    </button>
                </div>
            </form>
        </div>
    </div>

    <div class="card shadow-lg">
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>Payment ID</th>
                                <th>Date</th>
                                <th>Customer</th>
                                <th>Policy</th>
                                <th>Amount</th>
                                <th>Method</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var payment in Model)
                            {
                                <tr>
                                    <td>@payment.PaymentId</td>
                                    <td>@payment.PaymentDate.ToString("dd MMM yyyy")</td>
                                    <td>@payment.User?.FirstName @payment.User?.LastName</td>
                                    <td>@payment.Policy?.PolicyNumber</td>
                                    <td>₹@payment.Amount.ToString("N0")</td>
                                    <td>@payment.PaymentMethod</td>
                                    <td>
                                        <span class="badge @GetStatusBadge(payment.Status)">
                                            @payment.Status
                                        </span>
                                    </td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@payment.PaymentId" 
                                           class="btn btn-sm btn-outline-primary me-1" title="View Details">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@payment.PaymentId" 
                                           class="btn btn-sm btn-outline-warning" title="Edit Status">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                
                <div class="mt-4">
                    <nav aria-label="Payments pagination">
                        <ul class="pagination justify-content-center">
                            <li class="page-item disabled">
                                <a class="page-link" href="#" tabindex="-1">Previous</a>
                            </li>
                            <li class="page-item active"><a class="page-link" href="#">1</a></li>
                            <li class="page-item"><a class="page-link" href="#">2</a></li>
                            <li class="page-item"><a class="page-link" href="#">3</a></li>
                            <li class="page-item">
                                <a class="page-link" href="#">Next</a>
                            </li>
                        </ul>
                    </nav>
                </div>
            }
            else
            {
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    No payments found matching your criteria.
                </div>
            }
        </div>
    </div>
</div>

@functions {
    string GetStatusBadge(PaymentStatus status)
    {
        return status switch
        {
            PaymentStatus.Completed => "bg-success",
            PaymentStatus.Pending => "bg-warning",
            PaymentStatus.Failed => "bg-danger",
            PaymentStatus.Declined => "bg-secondary",
            PaymentStatus.Refunded => "bg-info",
            _ => "bg-secondary"
        };
    }
}